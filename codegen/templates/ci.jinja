let ci_s: HashSet<i32> =
    {{ zip_columns }}
    .filter_map(|{{ filter_map_closure|replace("'","")}}| {
        person_role_id
            .filter(|person_role_id| {{ join_conditions }})
            .map(|_| *movie_id)
    })
    .collect();