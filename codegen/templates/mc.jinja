let mc_s: HashSet<i32> =
    {{ zip_columns }}
    .filter_map(|{{ filter_map_closure|replace("'","")}}| {
        ({{ filter_conditions }} && {{ join_conditions }}).then_some(movie_id)
    })
    .collect();